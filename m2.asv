clear 
fs=10000;
f=697;
T=1/fs;
t=[0:T:1/f];
input=cos(2*pi*f.*t);
figure(1);
stem(t,input);
output=fftshift(fft(input));
figure(2);
stem([0:length(input)-1],abs(output));
%stem(linspace(-fs/2,fs/2,length(t)),abs(output));
N=length(input);
x=zeros(1,N);
for k=[1:N]
vk2=compute_vk(N-1,input,N,k);%THis gives vk(N-1)
vk=vk=xe(n)+2.*cos(2*pi*k/N).*compute_vk(n-1,xe,N,k)-compute_vk(n-2,xe,N,k);
x(k)=vk-(exp(-1i*2*pi/N*k))*vk2;
end
figure(3)
stem(abs(x));


